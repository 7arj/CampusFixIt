# Dependencies
node_modules/
package-lock.json   # Optional: Remove this line if you want to lock versions, but usually safe to keep for consistency
yarn.lock

# Environment Variables (CRITICAL: Never commit keys!)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Backend: Storage
backend/uploads/
*.log

# Backend: Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Frontend: Expo & React Native
.expo/
web-build/
dist/
.web/

# Frontend: Native Builds (If you ever run 'npx expo prebuild')
frontend/android/
frontend/ios/
*.jks
*.keystore
*.p12

# Windows System Files (Since you are on PC)
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# IDE / Editor Files
.vscode/
.idea/
*.swp
*.swo

# MacOS System Files (Good to have if a teammate uses Mac)
.DS_Store
.AppleDouble
.LSOverride